'use client';

export default function EditForm() {
  return (
    <div className="card">
      <h2>Team edit form temporarily disabled</h2>
      <p className="muted">
        Admin routing is working. Rebuild the full edit form after deployment is stable.
      </p>
    </div>
  );
}
